SELECT 
 
 ROW_NUMBER () OVER(PARTITION BY DT_FIM_ATENDIMENTO ORDER BY DT_INICIO_CONVERSA DESC) AS ACORDOS_DEDUPLICADOS
,NOME_CONTATO
,ID_ATENDIMENTO
,NOME_AGENTE
,TEMPO_ATENDIMENTO
,VOLUME_MENSAGENS
,QUALIFICACAO_FINAL
,ID_QUALIFICACAO
,COUNT(DISTINCT(ID_QUALIFICACAO)) AS QUANTIDADE_ACORDOS

FROM [ipo].[dbo].[WEON_LEVES_TOTAL] AS WLT
WHERE QUALIFICACAO_FINAL = 'ACORDO' AND NOME_AGENTE = 'ANA VITÃ“RIA DE JESUS PAES' --AND (NOME_CONTATO = 'Cadastro Sistema' OR NOME_CONTATO = 'ada')
GROUP BY
 NOME_AGENTE
,TEMPO_ATENDIMENTO
,VOLUME_MENSAGENS
,NOME_CONTATO
,QUALIFICACAO_FINAL
,ID_QUALIFICACAO
,ID_ATENDIMENTO
,PROTOCOLO
,DT_FIM_ATENDIMENTO
,DT_INICIO_CONVERSA
ORDER BY ACORDOS_DEDUPLICADOS 
